# Main environment: text pipelines, NLU baselines, Whisper speech experiments.
# Python 3.11+. Installed versions are those used for every number in the paper.
#
# PyTorch is pinned to the CUDA 12.6 build. Install it first, from the PyTorch
# index, or pip will resolve the CPU-only wheel and the speech experiments will
# run ~50x slower:
#   pip install torch==2.9.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cu126

torch==2.9.1
torchaudio==2.9.1

# Modelling
transformers==4.46.1
datasets==4.4.1
openai-whisper==20250625
sentencepiece==0.2.1

# Numerics / evaluation
numpy==2.3.5
scipy==1.16.3
scikit-learn==1.8.0
pandas==2.3.3

# Audio
librosa==0.11.0
soundfile==0.13.1

# Contamination metrics (experiments/contamination)
rouge-score==0.1.2
sacrebleu==2.6.0

# Text generation / translation (pipelines/*)
ollama==0.6.1
deep-translator==1.11.4
gTTS==2.5.4

# Speaker-similarity evidence (pipelines/voice_cloning)
Resemblyzer==0.1.4

tqdm==4.67.1
